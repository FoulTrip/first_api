from pymongo import MongoClient

# "MongoClient()" devuelve un objeto que guardamos en "connection" y que nos permite conectarnos a la base de datos e interctuar con todo su contenido
connection = MongoClient()